SELECT * FROM EMP;

SELECT 
DISTINCT JOB 
FROM EMP
WHERE SAL > 2000;

SELECT ENAME, SAL
FROM EMP e 
ORDER BY SAL DESC;

SELECT 
	ENAME, SAL
FROM EMP e
WHERE ROWNUM <= 5
ORDER BY SAL DESC;

SELECT * FROM (
SELECT * FROM EMP e 
ORDER BY SAL DESC
)
WHERE ROWNUM <= 5;







